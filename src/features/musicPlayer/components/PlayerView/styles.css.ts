import { style } from "@vanilla-extract/css";

export const OptionsContainer = style({
  display: "flex",
  flexDirection: "column",
});

export const PlayerProgressContainer = style({
  display: "flex",
  flexDirection: "column",
});
